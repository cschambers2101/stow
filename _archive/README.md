# _archive

Retired material, kept for reference only. Nothing here is stowed (see `.stow-local-ignore`), linted, or called by a live script.

| Folder | Retired | Why |
|--------|---------|-----|
| `install_programs/` | 5 Sep 2026 | qtile and Debian builds are no longer made; `nvidia_setup.sh`, `install_node.sh` and the SDDM theme installer duplicated what `../install_programs/` now does via `lib/` |
| `bin/` | 5 Sep 2026 | X11-only tools (i3lock, feh wallpaper, xrandr layouts, ffmpeg x11grab). The niri build is Wayland |
| `config/` | 5 Sep 2026 | qtile, i3, polybar and picom configs from the X11 era |

Older entries (`install_qtile.sh`, `install_required_programs_old.sh`, the two package lists) were already archived before this date; see `install_programs/README.md`.
